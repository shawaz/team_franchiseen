import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['striped-heron-973.convex.cloud', 'convex.storage', 'img.clerk.com', 'localhost'],
    
  },
};

export default nextConfig;